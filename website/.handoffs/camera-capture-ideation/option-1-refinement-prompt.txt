Edit the attached FOTOHAVN "Quiet Camera" UI design board into one refined version. This is the user-selected direction, not a request for new alternatives. Preserve the existing warm editorial visual language, natural photography, phone-camera-like shutter dock outside the photo, ready/capturing six-panel structure, mobile/iPad/desktop labels, and cream surfaces. Title "Quiet Camera." with small subtitle "Refined countdown placement". Use Cormorant Garamond and Manrope; #FBF8F2 #F3EBDD #1E1A17 muted brass #9A7A4F. No unapproved brand slogan. One independent high-quality concept image, target landscape 3000x2200. Each complete UI frame maintains natural proportions: mobile390x844, iPad834x1194, desktop1440x1024. No device bezels, status bars, notches, browser chrome, shadows or masks. The same existing Panorama Four 3:4 portrait photo template remains shown.

PRIMARY CHANGE: In the three bottom-row CAPTURING panels, REMOVE the countdown number from the cream header. Put ONE countdown numeral "3" OVER THE LIVE PHOTOGRAPH at its UPPER CENTER. It must be centered horizontally relative to the actual photo content, especially desktop's narrow portrait image, never centered relative to empty cream side gutters. Position badge ~16-24 logical pixels below the top edge of the photo on mobile and ~24-32px below on tablet/desktop. Compact circular or subtly rounded dark translucent ebony scrim, warm-white Manrope medium numeral, restrained soft contrast. At390px phone width use roughly56-64px backing with40-44px numeral; on tablet/desktop about80px backing with56px numeral. The result is a native-camera-like temporary countdown cue, clear at a glance. Only a local scrim behind the numeral, NO tint/dimming across the entire preview, NO full-screen veil, NO huge number in the center, NO progress wheel, NO bouncing animation representation. Number stays stationary as 3→2→1, then vanishes at shutter. Keep adults' faces visible; use natural headroom above subjects rather than shrinking faces to implausible scale. This position is a fixed design choice, not a face detection feature.

Header of capturing frames retains only FOTOHAVN, Exit, Photo2of4 and small four-step progress. Keep READY and CAPTURING header heights exactly the same within each device. Preview occupies IDENTICAL position and dimensions between states; do not resize/jump video when countdown appears. Ready frames have no overlaid countdown. During capture there is just Stop in the same fixed solid bottom dock; support controls hidden without the dock collapsing. Ready retains shutter, timer3s/5s/10s, Mirror and explicitly labeled Flip camera, source selector on desktop. No new controls or features.

Make the revised countdown placement immediately obvious in all three bottom panels. Add one tasteful magnified detail inset outside the app screens in the board's left annotation margin: cropped top of live image showing the number3 badge and thin annotation line to "Upper center, inside the preview". Keep this inset small enough not to crowd the six screens. Footer annotation text: "3 → 2 → 1 → capture" and "Temporary overlay. Bright preview. Stable framing." Add small note "Portrait template shown; the timer anchors to the photo on every crop." Keep all typography readable and elegant. The attached image is the edit target; preserve its chosen concept and change countdown placement and state-height consistency only.
